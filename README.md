# AutoParams Codespaces Demo

**Run in GitHub Codespaces** and demo AutoParams (JUnit 5 extension) with zero local setup.

## Quick Start
1. Click **Code → Create codespace on main**.
2. In the terminal:
   ```bash
   mvn -U clean test
